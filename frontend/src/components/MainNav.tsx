import { Button } from "./ui/button";

const MainNav = () => {
  return(
    <Button variant="ghost" className="font-bold text-white bg-orange-500 hover:bg-orange-700 hover:text-gray-300">Log In</Button>
  )
}

export default MainNav;